# English_words_frequency

## Prerequisites

Install the Python dependencies, then download the spaCy English model used for lemmatization:

```bash
python -m pip install -r requirements.txt
python -m spacy download en_core_web_sm
```
